{
    order forward_proxy before file_server
}
:PORT {
    forward_proxy {
        basic_auth USER PASS
        hide_ip
        hide_via
        probe_resistance
    }
    file_server {
        root /var/www/html
    }
}
